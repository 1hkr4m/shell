#!/bin/bash

UPATH=/home/igor/Test

ssh $1@$2 sudo 

for name in $(ls -l ~/Test | awk '/pub/{print $9}' | cut -d"_" -f1)
    do 
        useradd -m $name mkdir /home/$name/.ssh
        scp igor@192.33.33.10:$UPATH/$name_key.pub igor@192.33.33.17:/home/$name/.ssh
        cat $name_key >> known_hosts
    done

